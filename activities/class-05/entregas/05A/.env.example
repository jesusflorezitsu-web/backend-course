# Copiar a .env — la URL de TU backend de la clase 05 (el mismo que valida la
# boss battle). El backend debe tener FRONTEND_ORIGIN=http://localhost:5173
# (el origen EXACTO del dev server de Vite) para que CORS acepte las peticiones.
VITE_API_URL=http://localhost:3000